---
spec_id: SP-E2-06
title: "Спецификация реализации: производительность карусели и кэш изображений"
story_id: US-E2-06
status: drafted
updated: "2026-09-07"
---
# Спецификация реализации — US-E2-06 «Производительность карусели и кэш изображений»

## 1. Scope

**В объёме:**

* **Дисковый кэш изображений** карусели с явным верхним пределом **100–150 МБ** и политикой **LRU** (**A-21a**, **NFR-SR-6**).

* **Повторное использование кэша** при повторном открытии игры: изображения не грузятся заново с сети, если они в пределах лимита диска (**AC-01**, **A-21**).

* **Плавность листания** карусели без систематических фризов на целевых устройствах (**AC-02**, **NFR-1**, **NFR-SR-6**).

* **Согласование выбора библиотеки кэша** с tech lead (**ED-6**, **ED-13**, **ADR-004–009**) — как блокер/предусловие, а не самостоятельный выбор агента.

* **Prefetch** обложки/превью следующей карточки — **желательно**, не обязательно для первой итерации (**A-21a**).

* **Тесты**: unit-тесты кэша/виджета (TC-01, TC-03), ручная проверка плавности (TC-02).

**Вне scope:**

* Полная оптимизация всего приложения вне карусели (стори).

* Реализация самого экрана игры и карусели — **US-E2-03** (зависимость; здесь — только производительность и кэш поверх готовой карусели).

* Офлайн-доступ к контенту и Firestore persistence — **US-E2-05** (**A-20–A-22**); здесь затрагивается только дисковый кэш изображений.

* Обновление контента при сети — **US-E2-04**.

* Оптимизация веса/размеров изображений на стороне контента — **NFR-SR-4** (гайдлайн контента, **CR-5**).

## 2. Требования → шаги

| #  | Требование                                                          | Источник                     | Шаги |
| -- | ------------------------------------------------------------------- | ---------------------------- | ---- |
| R1 | Дисковый кэш изображений с верхним пределом 100–150 МБ, LRU        | AC-01, A-21a, NFR-SR-6       | 2    |
| R2 | Повторное открытие берёт изображения из кэша в пределах лимита     | AC-01, A-21                  | 2–3  |
| R3 | Листание без систематических фризов                                | AC-02, NFR-1, NFR-SR-6       | 3    |
| R4 | Выбор библиотеки кэша фиксирует tech lead (ADR)                    | ED-6, ED-13, ADR-004–009     | 1    |
| R5 | Prefetch следующей карточки (желательно)                           | A-21a                        | 3    |
| R6 | Лимит кэша не вытесняет критичные данные пользователя              | A-21a                        | 2    |

## 3. Архитектура данных

```mermaid
flowchart TD
  subgraph MP["МП / Flutter"]
    GS[Экран игры: карусель\nUS-E2-03]
    IC[Виджет кэшируемого изображения\ncached_network_image / эквивалент]
    DC[(Дисковый кэш изображений\n100–150 МБ, LRU)]
  end

  subgraph SRC["Supabase Storage (ED-14)"]
    OBJ[games/{id}/<file>]
  end

  GS -->|imageRef → supabasePublicUrl A-39| IC
  IC -->|нет в кэше → загрузка| OBJ
  IC -->|запись/чтение, LRU-вытеснение A-21a| DC
  OBJ -->|public URL A-39| IC
```

**Ключевые решения:**

* **Кэш на диске с верхним пределом** (**A-21a**, **NFR-SR-6**): изображения карусели кэшируются на диске; потолок **100–150 МБ**, политика **LRU**. Магазины приложений не задают фиксированного лимита — важны разумность и отсутствие злоупотреблений.

* **Библиотеку выбирает tech lead** (**ED-6**, **ED-13**, **ADR-004–009**): агент/разработчик не угадывает пакет до записи ADR. Ориентир — зрелый пакет с дисковым кэшем и LRU (`cached_network_image` или эквивалент, **A-21**).

* **Источник изображений — Supabase Storage** (**ED-14**, **A-39**): `Slide.imageRef` — путь объекта `games/{id}/<file>`; публичный URL — по правилу `supabasePublicUrl(path)`. Кэш работает поверх публичных URL, контракт данных не меняется.

* **Кэш не трогает данные пользователя** (**A-21a**): LRU-вытеснение затрагивает только кэш изображений; критичные данные пользователя не вытесняются.

## 4. Шаги (в порядке зависимостей)

### Шаг 1. Зафиксировать выбор библиотеки кэша (блокер)

* **ED-6 / ED-13 / ADR-004–009**: конкретный пакет для кэша изображений фиксирует **tech lead** в ADR. До записи ADR конкретный пакет не выбирается.

* Критерии выбора (по **A-21a**): (1) объём типичных превью и слайдов карусели; (2) не вытеснять критичные данные пользователя; (3) предсказуемость на слабых устройствах.

* Артефакт: запись в `docs/engineering-decisions-and-open-questions.md` (ADR-004–009) с указанием пакета и параметров кэша (лимит, LRU).

### Шаг 2. Интеграция дискового кэша изображений

* **Реализовано:** политика LRU-кэша с лимитом размера — `scenario/lib/cache/lru_cache.dart` (`LruCache`, `kImageCacheDefaultMaxBytes = 150 МБ`). Чистый Dart, не зависит от пакета (ED-13); покрыта unit-тестами `scenario/test/lru_cache_test.dart` (TC-01, TC-03).

* Реализация в `scenario/lib/` — обёртка над выбранным пакетом (шаг 1): виджет/сервис кэшируемого изображения, принимающий публичный URL (`supabasePublicUrl(imageRef)`, **A-39**). **Заблокировано** до записи ADR-004–009 (выбор пакета — за tech lead).

* Параметры кэша (**A-21a**, **NFR-SR-6**): верхний предел **100–150 МБ**, политика **LRU**; конфигурируемо (для тестов и dev — низкий лимит).

* Поведение: при наличии изображения в кэше в пределах лимита — чтение с диска без сетевого запроса (**AC-01**, **R2**); при отсутствии — загрузка и запись в кэш; при превышении лимита — LRU-вытеснение (**R6**).

* **Prefetch** (**A-21a**, **R5**): опционально, один-два HTTP-запроса на экран для обложки/превью следующей карточки; не обязательно для первой итерации.

### Шаг 3. Применение в карусели экрана игры

* Зависит от **US-E2-03** (экран игры с каруселью). В этой стори карусель использует кэшируемый виджет из шага 2 для слайдов `GamePublic.carousel` (`Slide.imageRef`).

* Плавность листания (**AC-02**, **NFR-1**, **NFR-SR-6**): изображения из кэша не блокируют листание; проверка по чеклисту QA на целевых устройствах (пороги — открытый вопрос стори, фиксируются в тест-плане).

### Шаг 4. Тесты

| TC    | Слой        | Проверка                                                       | Где                              |
| ----- | ----------- | -------------------------------------------------------------- | -------------------------------- |
| TC-01 | integration | Повторное открытие — изображения из кэша (0 HTTP при офлайн)   | unit кэша + ручная проверка      |
| TC-02 | manual-only | Листание без систематических фризов на целевом устройстве      | QA-чеклист (пороги — тест-план)  |
| TC-03 | integration | Лимит диска: LRU-вытеснение старых изображений, кэш не растёт  | unit кэша с низким лимитом в dev |

* Unit-тесты кэша (TC-01, TC-03): повторное чтение не делает сетевой запрос; при превышении лимита вытесняются старые (LRU); кэш не растёт бесконечно.

* TC-02 — ручная проверка плавности; автоматизация (FPS/perf overlay) — опционально, пороги в CI по профайлеру.

## 5. Файлы

* `docs/product/specs/e2-carousel-image-cache-performance.md` — **настоящий документ** (SP-E2-06).

* `docs/engineering-decisions-and-open-questions.md` — запись ADR-004–009 (выбор библиотеки кэша, шаг 1).

* `scenario/lib/cache/lru_cache.dart` — политика LRU-кэша с лимитом размера (реализовано, шаг 2).

* `scenario/test/lru_cache_test.dart` — unit-тесты политики кэша (реализовано, TC-01/TC-03).

* `scenario/lib/` — обёртка кэшируемого изображения (предлагается, шаг 2, блокер ED-13) и применение в карусели (шаг 3, зависит от US-E2-03).

* `scenario/pubspec.yaml` — зависимость на выбранный пакет кэша (после ADR, шаг 1).

* `scenario/lib/data/contract/models.dart` — `Slide.imageRef` (существует; контракт не меняется, **ED-14**).

* `scenario/test/` — unit-тесты кэша (TC-01, TC-03).

## 6. Риски

* **Выбор библиотеки кэша не зафиксирован** (**ED-13**, ADR-004–009) — блокер шага 2. Митиг: шаг 1 фиксирует критерии; конкретный пакет — за tech lead; до ADR не выбирается.

* **Экран игры/карусель не реализован** (**US-E2-03**) — шаг 3 невозможен до его завершения. Митиг: шаг 2 (кэш-виджет) реализуется и тестируется независимо; интеграция — после US-E2-03.

* **Пороговые устройства и FPS не зафиксированы** (открытый вопрос стори) — риск субъективной приёмки AC-02. Митиг: зафиксировать в тест-плане/QA-чеклисте; автоматизация FPS — опционально.

* **Кэш растёт бесконечно / вытесняет данные пользователя** (**A-21a**). Митиг: жёсткий верхний предел 100–150 МБ + LRU; кэш изолирован от данных пользователя.

## 7. Follow-up (вне scope)

* Реализация экрана игры и карусели — **US-E2-03**.

* Офлайн-доступ к контенту и Firestore persistence — **US-E2-05** (**A-20–A-22**).

* Обновление контента при сети — **US-E2-04**.

* Оптимизация веса/размеров изображений контента — **NFR-SR-4** (гайдлайн контента, **CR-5**).

## 8. Доступы и блокеры

* **ED-13 / ADR-004–009**: выбор библиотеки кэша — за tech lead (блокер шага 2).

* **US-E2-03**: экран игры с каруселью — зависимость шага 3.

* **A-40**: секреты (если пакет/сервис требует ключей) — только env/Secret Manager, не в git.

## 9. Verify

Соответствие критериям приёмки **US-E2-06**:

* **AC-01 (A-21a)** — TC-01/TC-03: при повторном открытии игры изображения берутся из кэша в пределах лимита диска (0 сетевых запросов при офлайн); при превышении лимита — LRU-вытеснение, кэш не растёт бесконечно.

* **AC-02 (NFR-SR-6, NFR-1)** — TC-02: листание карусели без систематических фризов по чеклисту QA на целевых устройствах (пороги — тест-план).

Критерий готовности: кэш-виджет с лимитом 100–150 МБ и LRU реализован и покрыт unit-тестами (TC-01, TC-03); интеграция в карусель экрана игры (после US-E2-03) не даёт систематических фризов (TC-02).

## 10. История изменений

| Дата       | Автор | Изменение                                                                                                                          |
| ---------- | ----- | ---------------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-07 | AID   | Первая версия (drafted). Дисковый кэш изображений 100–150 МБ/LRU по A-21a и NFR-SR-6; выбор библиотеки — ED-13/ADR-004–009; интеграция в карусель — после US-E2-03. |
| 2026-09-07 | AID   | Реализована политика LRU-кэша (`lib/cache/lru_cache.dart`) + unit-тесты (TC-01/TC-03); обёртка пакета и карусель остаются блокерами ED-13/US-E2-03. |